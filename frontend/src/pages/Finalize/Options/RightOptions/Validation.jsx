import { React } from "react";
import styles from "./RightOption.module.scss";

const Validation = () => {
  return (
    <div className={styles.container}>
      <button className={styles.button}>Generate an Updated Reframe</button>
    </div>
  );
};

export default Validation;
